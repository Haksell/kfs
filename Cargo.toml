[package]
name = "kfs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
lazy_static = { version = "1.5", features = ["spin_no_std"] }
spin = "0.12"
volatile = "0.3"
